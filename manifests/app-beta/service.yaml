apiVersion: v1
kind: Service
metadata:
  name: app-beta-service
  labels:
    app: app-beta
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: app-beta
