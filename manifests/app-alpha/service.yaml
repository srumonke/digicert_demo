apiVersion: v1
kind: Service
metadata:
  name: app-alpha-service
  labels:
    app: app-alpha
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: app-alpha
